Backend removed from runtime by stubbing server/database.

- Original files: `backend/server.js` and `backend/database.js`.
- Current repository includes Supabase schema: `college_management_system_postgres.sql`.

If you need to re-enable the backend, restore the original contents of server/database.

